/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
      domains: ['geekyglam.in', 'img.freepik.com', 'cdn.pixabay.com', 'tse4.mm.bing.net', 'd3njjcbhbojbot.cloudfront.net'],
    },
}

module.exports = nextConfig
